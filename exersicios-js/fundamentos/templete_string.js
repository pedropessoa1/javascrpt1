const nome = "Rebeca"
const concatenacao = "Ola" + nome + "!"
const temlpete = `
     Ola
    ${nome}!`
console.log(concatenacao, temlpete)

// espressoes...
console.log(`1 + 1 = ${1 + 1}`)

const up = texto => texto.toUpperCase()
console.log(`Ei... ${up(`cuidado`)}!`)